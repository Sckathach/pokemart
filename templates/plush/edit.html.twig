{% extends 'base.html.twig' %}

{% block title %}Edit Plush{% endblock %}

{% block info %}
    {{ include('plush/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('plush_index') }}">back to list</a>

    {{ include('plush/_delete_form.html.twig') }}
{% endblock %}
